# Attribution

This skill is a verbatim copy of the **ffmpeg-toolkit** skill from:

- Source: https://github.com/jakenuts/agent-skills (`skills/ffmpeg-toolkit/`)
- Author: jakenuts

It is included here as the upstream knowledge base. The adapted, Docker-aware
variant for this MCP server lives in `../ffmpeg-win/` and routes all commands
through the `ffmpeg-win` MCP tool (Windows path conversion) instead of a local
FFmpeg binary.
